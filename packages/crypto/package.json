{
  "name": "@vidsandgifs/crypto",
  "version": "0.0.0",
  "description": "Private crypto helpers for vidsandgifs — HMAC URL signing + link-token packing for Telegram and Discord. Lives in this monorepo for the owner's local dev; before making the public repo public, move this package to a private GitHub repo and publish to GitHub Packages so cloners can't pnpm install.",
  "private": true,
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "bin": {
    "sign-license": "./dist/scripts/sign-license.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsc -p . --watch --preserveWatchOutput",
    "typecheck": "tsc --noEmit",
    "sign-license": "node ./dist/scripts/sign-license.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.3"
  }
}
