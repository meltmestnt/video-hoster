// Stub for Next.js's `server-only` package. The real one is a runtime
// guard that throws if imported from a client component; under vitest
// we don't need that enforcement, so the module just exports nothing.
export {};
